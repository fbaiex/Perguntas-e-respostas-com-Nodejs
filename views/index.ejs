<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>YahoX</title>
</head>
<body>
    <h1>Perguntas e respostas no YahoX</h1>
    <hr/>
    <h3>Lista com as perguntas</h3>
    <hr/>
    <p>Nome: <%= nome %></p>
    <p>Linguagem de programação favorita: <%= lang %></p>
    <p>Empresa: <%= empresa %></p>
    <p>Quantidade de alunos: <%= inscritos %></p>

    <% if(msg == true){ %>
        <h3>Isso é uma msg de erro!</h3>
    <%} else { %>
        <h3>Nenhum erro!</h3>
        <% } %>

    <% produtos.forEach(function(produto) { %>
        
    <% }) %>    
    
</body>
</html>